

This folder contains various files included in the installation. 

It's also used when running debug or release versions as the application folder, so the program can find a Template.neo file, for instance. 